interface:
  display_name: "Codex"
  short_description: "Repo-specific patterns and workflows for codex"
  default_prompt: "Use the codex repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true